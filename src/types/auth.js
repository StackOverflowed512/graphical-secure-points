
// These are the type definitions for auth-related data
// Since we're using JSX, these are just comments for documentation

/*
User {
  id: string;
  username: string;
  email: string;
  token?: string;
}

ClickPoint {
  x: number;
  y: number;
  imageId: string;
}

AuthImage {
  id: string;
  url: string;
  alt: string;
}

RegisterData {
  username: string;
  email: string;
  clickPoints: ClickPoint[];
}

LoginData {
  email: string;
  clickPoints: ClickPoint[];
}

AuthContextType {
  user: User | null;
  loading: boolean;
  error: string | null;
  isAuthenticated: boolean;
  register: (data: RegisterData) => Promise<void>;
  login: (data: LoginData) => Promise<void>;
  logout: () => void;
}
*/
